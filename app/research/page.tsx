import Link from 'next/link'
import {getPublications} from '@/lib/sanity'

export default async function ResearchPage(){const pubs=await getPublications();return <main className="section"><div className="wrap"><div className="eyebrow">Research repository</div><h2>StoneComms Research & Intelligence</h2><p style={{maxWidth:760,color:'#47585b'}}>Independent, decision-useful research for Africa’s climate economy. Publications are structured for a growing repository and permanent citation.</p><div className="research-grid" style={{marginTop:36}}>{pubs.map(p=><article className="card" key={p._id}><div className="eyebrow">{p.publicationType?.replaceAll('-',' ') || 'Research'}</div><h3><Link href={`/research/${p.slug}`}>{p.title}</Link></h3><p>{p.standfirst}</p><div className="meta">{p.publicationDate} · {p.byline || 'StoneComms Research & Intelligence'}</div></article>)}</div></div></main>}
